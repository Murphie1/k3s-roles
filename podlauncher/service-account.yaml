apiVersion: v1
kind: ServiceAccount
metadata:
  name: podlauncher-sa-207-211-161-111
  namespace: podlauncher
  labels:
    app.kubernetes.io/name: podlauncher
    app.kubernetes.io/instance: podlauncher-207-211-161-111
    app.kubernetes.io/managed-by: agent
  annotations:
    # Ensures logs/auditing can trace workloads back to this SA
    iam.gke.io/gcp-service-account: "" # (only if using GKE + Workload Identity)
    eks.amazonaws.com/role-arn: ""    # (only if using EKS IRSA)
    # you can add audit annotations like:
    audit.openpolicyagent.org/trace: "true"
automountServiceAccountToken: false  # 🔒 prevents accidental token mounting unless needed
